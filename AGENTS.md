# Agent Guidelines

> Single source of truth for all AI coding assistants (Claude Code, Cursor, GitHub Copilot, etc.).

**Read [@CLAUDE.md](CLAUDE.md) for full project context, conventions, and constraints.** This file exists so non-Claude agents (notably Cursor via `.cursor/rules/general.mdc`) load the same canonical context.

If you are editing this file: only add agent-specific overrides here. Anything that applies to *all* agents goes in CLAUDE.md.
